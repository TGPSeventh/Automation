{
    "name": "Automation Environment",
    "image": "mcr.microsoft.com/devcontainers/python:3.10",  // Use a specific image if needed
    "postCreateCommand": "pip install -r requirements.txt",  // Install dependencies
    "extensions": [
      "ms-python.python",  // Python extension
      "ms-azuretools.vscode-docker",  // Docker tools if required
      "eamodio.gitlens"  // Git extensions for better control
    ]
  }